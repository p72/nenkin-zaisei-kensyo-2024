# -*- coding: utf-8 -*-
"""
厚生年金/給付費推計/common/_const.h と _kx.h の定数
=====================================================
②は定数がたった9個しかない。年齢・区分の境目はソースに直接書いてある
（`if( x >= 65 )` のように）ので、定数表からは読み取れない。

    STTY  15   推計の開始年度（西暦 − 2000）
    ENDY 125   推計の最終年度（2125年度）
    ECSTY -2   経済前提の開始年度（1998年度）
    ECEDY 128  経済前提の最終年度（2128年度。推計より3年長い）
    KS    20   基準年度の前年
    KE   125   ループの最終年度
    KIJUN 21   基準年度（2021年度）
    FLKS  15   FLCK（ファイル整合性の確認）の開始年度
    FLKE 125   同・最終年度
    WKKS  21   外枠の開始年度
    WKKE 125   同・最終年度

`C19(kx)` は `kx + 100`
-----------------------
`common/_kx.h` の唯一のマクロ。

```c
#define C19(kx) (kx+100)
```

②は年度を「西暦 − 2000」で持つので、1900年代は**負**になる
（1954年度 = -46）。配列の添字に負は使えないので、`C19()` で 100 を
足して下駄をはかせる。つまり `pre[C19(-54)]` は「1946年度から
2000年度までの 55 要素」を意味する。

    C19(-54) = 46      1946年度
    C19(0)   = 100     2000年度
    C19(125) = 225     2125年度

年度の原点が2つある
-------------------
    人数・給付費   `k`（西暦 − 2000。0〜125）
    経済前提       `kx`（同じだが `ECSTY = -2` から。配列は C19 で下駄）

①（西暦そのまま）④（2つの原点）⑤⑥（西暦 − 2000）と、システムごとに
違う。②は⑤⑥と同じ「西暦 − 2000」だが、負の年度を扱う点だけが違う。
"""

__all__ = [
    "STTY", "ENDY", "ECSTY", "ECEDY", "KS", "KE", "KIJUN",
    "FLKS", "FLKE", "WKKS", "WKKE", "BUF_SIZE", "C19",
]

STTY = 15
ENDY = 125

ECSTY = -2
ECEDY = 128

KS = 20
KE = 125

KIJUN = 21

FLKS = 15
FLKE = 125

WKKS = 21
WKKE = 125

BUF_SIZE = 10000


def C19(kx):
    """common/_kx.h の `#define C19(kx) (kx+100)`。"""
    return kx + 100
